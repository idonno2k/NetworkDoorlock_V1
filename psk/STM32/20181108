파일 구조는
2자리/3자리/RF번호(앞 2자리 제외한 6자리) 로 구성
SETTING/SYNCDATE  파일에 시간정보 저장
SETTING 폴더에 다른 기본 정보도 저장할 예정

처음 SD카드를 수기로 셋팅해줘야 하기때문에
초기화 정보는 서버에서 파일을 생성해서 SD카드로 복사해서 사용( 초기화 시간단축 )
초기화 정보를 복사 못 했을 경우에는 SETTING/SYNCDATE 파일의 값이 없기때문에 전체 파일을 받아야 함


sprintf(paramStr, "?sNo=%d&sCount=%d&SyncDate=%s",sNo,sCnt, SyncDateStr); 
이 부분 자료형 변환이 잘 안됨...

초기화 셋팅이 아니더라고 5분간격으로 동기화 하는 동안에 
데이타가 많지 않아서 오래 걸리지는 않겠지만 그래도 만약을 대비해서
멀티쓰레드 라이브러리 적용 후 테스트 예정
(동기화 중간에도 카드 태그가 가능해야 함)

